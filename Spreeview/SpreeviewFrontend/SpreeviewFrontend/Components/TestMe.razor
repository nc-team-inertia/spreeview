<h3>TestMe</h3>

@code {
    Theme.UniverseThemes.UniversalTheme ut = new(Theme.UniverseThemes.Themes.Light);

    protected override void OnParametersSet()
    {
        base.OnParametersSet();

        //injects css of other class?

        Console.WriteLine(ut.fBgCol);
    }
}

